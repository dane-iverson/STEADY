import React from "react";
import ReactDOM from "react-dom/client";
import App from "./steady-t1dm-app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
